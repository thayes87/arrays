#string array
bike_brands = ["trek,", "specialized", "cannondale", "giant"]
bike_frame_sizes = [17, 19, 21, 23]
bike_wheel_sizes = [24.0, 26.0, 27.5, 29.0]
bike_booleans = [true, false, false, true]

#identifies last element from array and returns it
bike_brands.pop

#identifies element[0] and returns it, shifting all other elements up
bike_frame_sizes.shift

#allows addition of and element to the end of the array
bike.push("norco", "revel")

#allows addition of an element as element[0]
bike_wheel_sizes.unshift(20.0)
